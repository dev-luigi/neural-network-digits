"""
Neural network from scratch: a neural network written in Python + NumPy that learns to recognize handwritten
digits (MNIST), with an interface to watch it learn and look inside it.

    neural_net/    the network, the data, the training and the charts (no interface)
    gui/           the graphical interface, one file per tab
    assistant/     the assistant: search engine, knowledge and hints
    cli.py         the terminal commands (start.py and the neural-network-digits command start from here)
    i18n.py        the languages (translations in locales/)
    updater.py     the updates from GitHub
    project.py     name, version, author, links
"""
